<%= simple_form_for(@follow, url: "/relationships/follow_user/#{user.id}",
    data: {
      relationship_target: "follow",
      action: "submit->relationship#follow"
    },
    html: { class: hidden_class }
  )  do |f| %>
    <input type="hidden" name="follow[user_id]" data-user-id="<%= user.id %>">

    <%= button_tag( :class => 'follow-button' ) do %>
    <i class="fa-solid fa-user-plus" style="color: #ffffff;"></i>
    <% end %>
  <% end %>
